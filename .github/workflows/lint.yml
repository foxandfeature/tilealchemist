name: Lint

on:
  push:
  pull_request:

jobs:
  comment-style:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Check comment style
        run: python3 lint/comment_style.py tilealchemist lint
